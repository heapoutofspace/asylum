## 1. Implementation

- [x] 1.1 Rewrite `cmd/asylum/main.go` with argument parsing, config loading, image ensure, container assembly, and syscall.Exec
- [x] 1.2 Verify `go build ./...` passes and `make build` produces a working binary

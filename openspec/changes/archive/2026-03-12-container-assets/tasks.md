## 1. Dockerfile

- [x] 1.1 Replace `assets/Dockerfile` with full Asylum Dockerfile adapted from AgentBox (add Gemini CLI, Codex, remove openspec, rename labels)

## 2. Entrypoint

- [x] 2.1 Replace `assets/entrypoint.sh` with Asylum-adapted entrypoint (ASYLUM_ env vars, updated banner)

## 3. Verify

- [x] 3.1 Verify `go build ./...` passes (embed still works)
